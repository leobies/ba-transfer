BA Transfer Brasil — v20
This version fixes the persistent hero issues:
- Only ONE hero background image (assets/hero-bg.png)
- Only ONE right bubble (no duplicates)
- Both bubbles are placed as GRID items aligned to the bottom area (no absolute floating layers),
  so they don't cover the illustration (buildings/auto/person).
- Glass style kept with vivid look, flags included.
WhatsApp: 5547988288907
